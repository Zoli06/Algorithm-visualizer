"use strict"

let numbers = [];
let numbersLength = 20;

function generateRandomNumbers() {
    for (let i = 0; i < numbersLength; i++) {
        numbers.push(Math.random());
    }
}

function drawNumbers() {
}